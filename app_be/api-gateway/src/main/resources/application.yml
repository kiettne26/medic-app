server:
  port: 8080

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      routes:
        # Auth Service
        - id: auth-service
          uri: lb://auth-service
          predicates:
            - Path=/api/auth/**
          filters:
            - StripPrefix=1

        # User Service
        - id: user-service
          uri: lb://user-service
          predicates:
            - Path=/api/users/**, /api/profiles/**, /api/doctors/**, /api/services/**, /api/reviews/**
          filters:
            - StripPrefix=1

        # Chat API (no StripPrefix - ChatController uses /api/chat/*)
        - id: user-service-chat
          uri: lb://user-service
          predicates:
            - Path=/api/chat/**

        # WebSocket for Chat (no StripPrefix)
        - id: user-service-ws
          uri: lb://user-service
          predicates:
            - Path=/ws/**

        # Booking Service
        - id: booking-service
          uri: lb://booking-service
          predicates:
            - Path=/api/bookings/**, /api/slots/**
          filters:
            - StripPrefix=1

        # WebSocket for Booking Service (real-time slot notifications)
        - id: booking-service-ws
          uri: lb:ws://booking-service
          predicates:
            - Path=/ws/booking/**

        # Notification Service
        - id: notification-service
          uri: lb://notification-service
          predicates:
            - Path=/api/notifications/**
          filters:
            - StripPrefix=1

        # Analytics Service
        - id: analytics-service
          uri: lb://analytics-service
          predicates:
            - Path=/api/analytics/**, /api/reports/**
          filters:
            - StripPrefix=1

      # Global CORS
      globalcors:
        cors-configurations:
          '[/**]':
            allowedOrigins:
              - "http://localhost:3000"
              - "http://localhost:5173"
              - "*"
            allowedMethods:
              - GET
              - POST
              - PUT
              - DELETE
              - PATCH
              - OPTIONS
            allowedHeaders: "*"
            allowCredentials: false
            maxAge: 3600

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,gateway
